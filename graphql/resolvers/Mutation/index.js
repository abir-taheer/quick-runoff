import addOption from "./addOption";
import login from "./login";
import rank from "./rank";
import disableOption from "./disableOption";

const Mutation = {
	addOption,
	disableOption,
	login,
	rank
};

export default Mutation;